import { IUser } from "@/client/store/types";

export default function Page(){
  return <div>

  </div>
}

function UserForm({user, disable = false}:{user?:IUser, disable:boolean}){
  if(user){}
  else{
    return 
  }
}